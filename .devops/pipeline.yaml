trigger: none

resources:
  pipelines:
  - pipeline: renew
    source: web.temporal.internal.dev.p4pa.pagopa.it
    trigger: 
      branches:
        include: 
        - test-pipeline
variables:
  a: $(resources.pipeline.renew.force_renew_cert)      

pool:
  vmImage: ubuntu-latest

jobs:
  - job: A1 
    steps:
      - script: |
          echo $(a)
