trigger: none

resources:
  pipelines:
  - pipeline: renew
    source: web.temporal.internal.dev.p4pa.pagopa.it
    trigger: 
      branches:
        include: 
        - test-pipeline
  
pool:
  vmImage: ubuntu-latest

jobs:
- job: useOutput
  steps:
  - download: renew
    artifact: shared-output

  - script: |
      echo "Leggo la versione dal file:"
      VERSION=$(cat $(Pipeline.Workspace)/pipelineA/shared-output/version.txt)
      echo "VERSION = $VERSION"