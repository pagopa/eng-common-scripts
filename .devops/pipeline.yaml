trigger: none

resources:
  pipelines:
  - pipeline: renew
    source: web.temporal.internal.dev.p4pa.pagopa.it
    trigger: 
      branches:
        include: 
        - master
variables:
  a: $[ resources.pipeline.renew.pipelineID ]        

pool:
  vmImage: ubuntu-latest

jobs:
  - job: A1 
    steps:
      - script: |
          echo $(a)
          echo $[ resources.pipeline.renew.pipelineID ]
