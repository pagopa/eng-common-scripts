trigger: none

resources:
  pipelines:
  - pipeline: renew
    source: web.temporal.internal.dev.p4pa.pagopa.it
    project: p4pa-projects
    trigger:
      branches:
        - master

jobs:
- job: echo
  steps:
  - script: echo "TEST"
