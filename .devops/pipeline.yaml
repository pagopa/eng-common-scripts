trigger: none

resources:
  pipelines:
  - pipeline: renew
    source: web.temporal.internal.dev.p4pa.pagopa.it
    trigger: 
      branches:
        include: 
        - master

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    echo "##vso[task.setvariable variable=resources.pipeline.renew.force_renew_cert]$force_renew_cert"
