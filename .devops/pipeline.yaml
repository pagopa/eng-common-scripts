trigger: none

resources:
  pipelines:
  - pipeline: renew
    source: web.temporal.internal.dev.p4pa.pagopa.it
    trigger: 
      branches:
        include: 
        - master

pool:
  vmImage: ubuntu-latest

jobs:
- job: echo
  steps:
  - script: echo $(resources.pipeline.renew.force_renew_cert)
